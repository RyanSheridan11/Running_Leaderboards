<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Running Leaderboard" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="top-nav">
      <div class="nav-container">
        <%= link_to "5k Leaderboard", root_path, class: "nav-brand" %>
        <div class="nav-links">
          <% if logged_in? %>
            <span class="nav-user">Hello, <%= current_user.username %>!</span>
            <%= link_to "Add Run", new_run_path, class: "nav-link" %>
            <%= link_to "My Profile", profile_path, class: "nav-link" %>
            <% if current_user.admin? %>
              <div class="dropdown">
                <button class="nav-link dropdown-toggle">Admin</button>
                <div class="dropdown-menu">
                  <%= link_to "Manage Runs", admin_runs_path, class: "dropdown-item" %>
                  <%= link_to "Manage Users", admin_users_path, class: "dropdown-item" %>
                  <%= link_to "Manage Deadlines", admin_race_deadlines_path, class: "dropdown-item" %>
                </div>
              </div>
            <% end %>
            <%= link_to "Log out", logout_path, data: { turbo_method: :delete }, class: "nav-link" %>
          <% else %>
            <%= link_to "Log in", login_path, class: "nav-link" %>
            <%= link_to "Sign up", new_user_path, class: "nav-link" %>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="main-content">
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="alert alert-error"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
